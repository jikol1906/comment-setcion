import * as React from 'react';
import CommentReplyThread from '../../Layout/CommentReplyThread/CommentReplyThread';

interface ICommentReplyListProps {
}

const CommentReplyList: React.FunctionComponent<ICommentReplyListProps> = (props) => {
  return <CommentReplyThread>
      
  </CommentReplyThread>;
};

export default CommentReplyList;
